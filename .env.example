DATABASE_URL="file:./dev.db"

# STT provider: "mock" (default) or "deepgram" (requires STT_API_KEY)
# STT_PROVIDER=mock

# Deepgram API key — required when STT_PROVIDER=deepgram
# Get one at https://console.deepgram.com
# STT_API_KEY=

# STT rate limiting (per IP, defaults for 30-50 beta users)
# STT_RATE_LIMIT_PER_MINUTE=10
# STT_RATE_LIMIT_PER_HOUR=50
# STT_GLOBAL_RATE_LIMIT_PER_HOUR=500

# TTS provider: "mock" (default) or "google" (requires TTS_API_KEY)
# TTS_PROVIDER=mock

# Google Cloud Text-to-Speech API key (required when TTS_PROVIDER=google)
# Get one at https://console.cloud.google.com/apis/credentials
# TTS_API_KEY=

# LLM_API_KEY=
